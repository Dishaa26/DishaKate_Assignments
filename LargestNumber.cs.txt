using System;

class Program
{
    static void Main()
    {
        int[] arr = { 10, 25, 3, 99, 48 };

        int max = arr[0];

        foreach (int num in arr)
        {
            if (num > max)
                max = num;
        }

        Console.WriteLine("Largest Number = " + max);
    }
}